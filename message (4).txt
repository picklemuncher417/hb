local Players = cloneref(game:GetService('Players'))
local RS = cloneref(game:GetService('RunService'))
local Client = Players.LocalPlayer

-- Default XYZ sizing parameters (Now allows values down to 1)
local sizeX = 5
local sizeY = 5
local sizeZ = 5
local hitBoxTransparency = 1 
local hitboxEnabled = true

local ScreenGui = Instance.new('ScreenGui')
ScreenGui.Name = 'HitboxExpanderUI'
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = Client:WaitForChild('PlayerGui')

local MainFrame = Instance.new('Frame')
MainFrame.Size = UDim2.new(0, 250, 0, 310) 
MainFrame.Position = UDim2.new(0.5, -125, 0.05, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
MainFrame.BorderSizePixel = 0
MainFrame.Parent = ScreenGui

local DragDetector = Instance.new('UIDragDetector')
DragDetector.Parent = MainFrame

local UICorner = Instance.new('UICorner')
UICorner.CornerRadius = UDim.new(0, 8)
UICorner.Parent = MainFrame

local Title = Instance.new('TextLabel')
Title.Size = UDim2.new(1, 0, 0, 30)
Title.BackgroundTransparency = 1
Title.Text = 'AlphasHitboxExpander'
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 16
Title.Parent = MainFrame

---------------------------------------------------------
-- MASTER TOGGLE INTERFACE ELEMENT
---------------------------------------------------------
local ToggleButton = Instance.new('TextButton')
ToggleButton.Size = UDim2.new(1, -20, 0, 25)
ToggleButton.Position = UDim2.new(0, 10, 0, 35)
ToggleButton.BackgroundColor3 = Color3.fromRGB(0, 170, 100)
ToggleButton.Text = 'Hitbox: ON'
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.Font = Enum.Font.GothamBold
ToggleButton.TextSize = 12
ToggleButton.Parent = MainFrame

local ToggleCorner = Instance.new('UICorner')
ToggleCorner.CornerRadius = UDim.new(0, 6)
ToggleCorner.Parent = ToggleButton

---------------------------------------------------------
-- X-AXIS CONTROLLERS
---------------------------------------------------------
local LabelX = Instance.new('TextLabel')
LabelX.Size = UDim2.new(1, -20, 0, 15)
LabelX.Position = UDim2.new(0, 10, 0, 65)
LabelX.BackgroundTransparency = 1
LabelX.Text = 'Size X: ' .. sizeX
LabelX.TextColor3 = Color3.fromRGB(200, 200, 200)
LabelX.Font = Enum.Font.Gotham
LabelX.TextSize = 12
LabelX.TextXAlignment = Enum.TextXAlignment.Left
LabelX.Parent = MainFrame

local FrameX = Instance.new('Frame')
FrameX.Size = UDim2.new(1, -20, 0, 20)
FrameX.Position = UDim2.new(0, 10, 0, 80)
FrameX.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
FrameX.BorderSizePixel = 0
FrameX.Parent = MainFrame

Instance.new('UICorner', FrameX).CornerRadius = UDim.new(0, 6)

local ButtonX = Instance.new('TextButton')
ButtonX.Size = UDim2.new(0, 40, 1, 0)
ButtonX.Position = UDim2.new((sizeX - 1) / 99, -20, 0, 0)
ButtonX.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
ButtonX.BorderSizePixel = 0
ButtonX.Text = ''
ButtonX.Parent = FrameX

Instance.new('UICorner', ButtonX).CornerRadius = UDim.new(0, 6)

---------------------------------------------------------
-- Y-AXIS CONTROLLERS
---------------------------------------------------------
local LabelY = Instance.new('TextLabel')
LabelY.Size = UDim2.new(1, -20, 0, 15)
LabelY.Position = UDim2.new(0, 10, 0, 110)
LabelY.BackgroundTransparency = 1
LabelY.Text = 'Size Y: ' .. sizeY
LabelY.TextColor3 = Color3.fromRGB(200, 200, 200)
LabelY.Font = Enum.Font.Gotham
LabelY.TextSize = 12
LabelY.TextXAlignment = Enum.TextXAlignment.Left
LabelY.Parent = MainFrame

local FrameY = Instance.new('Frame')
FrameY.Size = UDim2.new(1, -20, 0, 20)
FrameY.Position = UDim2.new(0, 10, 0, 125)
FrameY.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
FrameY.BorderSizePixel = 0
FrameY.Parent = MainFrame

Instance.new('UICorner', FrameY).CornerRadius = UDim.new(0, 6)

local ButtonY = Instance.new('TextButton')
ButtonY.Size = UDim2.new(0, 40, 1, 0)
ButtonY.Position = UDim2.new((sizeY - 1) / 99, -20, 0, 0)
ButtonY.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
ButtonY.BorderSizePixel = 0
ButtonY.Text = ''
ButtonY.Parent = FrameY

Instance.new('UICorner', ButtonY).CornerRadius = UDim.new(0, 6)

---------------------------------------------------------
-- Z-AXIS CONTROLLERS
---------------------------------------------------------
local LabelZ = Instance.new('TextLabel')
LabelZ.Size = UDim2.new(1, -20, 0, 15)
LabelZ.Position = UDim2.new(0, 10, 0, 155)
LabelZ.BackgroundTransparency = 1
LabelZ.Text = 'Size Z: ' .. sizeZ
LabelZ.TextColor3 = Color3.fromRGB(200, 200, 200)
LabelZ.Font = Enum.Font.Gotham
LabelZ.TextSize = 12
LabelZ.TextXAlignment = Enum.TextXAlignment.Left
LabelZ.Parent = MainFrame

local FrameZ = Instance.new('Frame')
FrameZ.Size = UDim2.new(1, -20, 0, 20)
FrameZ.Position = UDim2.new(0, 10, 0, 170)
FrameZ.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
FrameZ.BorderSizePixel = 0
FrameZ.Parent = MainFrame

Instance.new('UICorner', FrameZ).CornerRadius = UDim.new(0, 6)

local ButtonZ = Instance.new('TextButton')
ButtonZ.Size = UDim2.new(0, 40, 1, 0)
ButtonZ.Position = UDim2.new((sizeZ - 1) / 99, -20, 0, 0)
ButtonZ.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
ButtonZ.BorderSizePixel = 0
ButtonZ.Text = ''
ButtonZ.Parent = FrameZ

Instance.new('UICorner', ButtonZ).CornerRadius = UDim.new(0, 6)

---------------------------------------------------------
-- TRANSPARENCROLLER WIDGET
---------------------------------------------------------
local TransLabel = Instance.new('TextLabel')
TransLabel.Size = UDim2.new(1, -20, 0, 15)
TransLabel.Position = UDim2.new(0, 10, 0, 205)
TransLabel.BackgroundTransparency = 1
TransLabel.Text = 'Transparency: ' .. math.round(hitBoxTransparency * 100) .. '%'
TransLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
TransLabel.Font = Enum.Font.Gotham
TransLabel.TextSize = 12
TransLabel.TextXAlignment = Enum.TextXAlignment.Left
TransLabel.Parent = MainFrame

local TransSliderFrame = Instance.new('Frame')
TransSliderFrame.Size = UDim2.new(1, -20, 0, 20)
TransSliderFrame.Position = UDim2.new(0, 10, 0, 220)
TransSliderFrame.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
TransSliderFrame.BorderSizePixel = 0
TransSliderFrame.Parent = MainFrame

Instance.new('UICorner', TransSliderFrame).CornerRadius = UDim.new(0, 6)

local TransSliderButton = Instance.new('TextButton')
TransSliderButton.Size = UDim2.new(0, 40, 1, 0)
TransSliderButton.Position = UDim2.new(hitBoxTransparency, -20, 0, 0)
TransSliderButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
TransSliderButton.BorderSizePixel = 0
TransSliderButton.Text = ''
TransSliderButton.Parent = TransSliderFrame

Instance.new('UICorner', TransSliderButton).CornerRadius = UDim.new(0, 6)

---------------------------------------------------------
-- INPUT STATE SEPARATION MANAGEMENT
---------------------------------------------------------
local dragX, dragY, dragZ, dragTrans = false, false, false, false

ButtonX.MouseButton1Down:Connect(function() dragX = true end)
ButtonY.MouseButton1Down:Connect(function() dragY = true end)
ButtonZ.MouseButton1Down:Connect(function() dragZ = true end)
TransSliderButton.MouseButton1Down:Connect(function() dragTrans = true end)

game:GetService('UserInputService').InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragX, dragY, dragZ, dragTrans = false, false, false, false
    end
end)

RS.RenderStepped:Connect(function()
    local mouse = Client:GetMouse()
    
    if dragX then
        local pct = math.clamp(mouse.X - FrameX.AbsolutePosition.X, 0, FrameX.AbsoluteSize.X) / FrameX.AbsoluteSize.X
        sizeX = math.floor(1 + (pct * 99)) -- Can drop down to a scale of 1
        ButtonX.Position = UDim2.new(pct, -20, 0, 0)
        LabelX.Text = 'Size X: ' .. sizeX
    end
    if dragY then
        local pct = math.clamp(mouse.X - FrameY.AbsolutePosition.X, 0, FrameY.AbsoluteSize.X) / FrameY.AbsoluteSize.X
        sizeY = math.floor(1 + (pct * 99)) -- Can drop down to a scale of 1
        ButtonY.Position = UDim2.new(pct, -20, 0, 0)
        LabelY.Text = 'Size Y: ' .. sizeY
    end
    if dragZ then
        local pct = math.clamp(mouse.X - FrameZ.AbsolutePosition.X, 0, FrameZ.AbsoluteSize.X) / FrameZ.AbsoluteSize.X
        sizeZ = math.floor(1 + (pct * 99)) -- Can drop down to a scale of 1
        ButtonZ.Position = UDim2.new(pct, -20, 0, 0)
        LabelZ.Text = 'Size Z: ' .. sizeZ
    end
    if dragTrans then
        local pct = math.clamp(mouse.X - TransSliderFrame.AbsolutePosition.X, 0, TransSliderFrame.AbsoluteSize.X) / TransSliderFrame.AbsoluteSize.X
        hitBoxTransparency = pct
        TransSliderButton.Position = UDim2.new(pct, -20, 0, 0)
        TransLabel.Text = 'Transparency: ' .. math.round(hitBoxTransparency * 100) .. '%'
    end
end)

ToggleButton.MouseButton1Click:Connect(function()
    hitboxEnabled = not hitboxEnabled
    if hitboxEnabled then
        ToggleButton.Text = 'Hitbox: ON'
        ToggleButton.BackgroundColor3 = Color3.fromRGB(0, 170, 100)
    else
        ToggleButton.Text = 'Hitbox: OFF'
        ToggleButton.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
    end
end)

---------------------------------------------------------
-- COMPREHENSIVE SIMULATION RUNTIME ENVIRONMENT
---------------------------------------------------------
RS.RenderStepped:Connect(function()
    for _, Player in pairs(Players:GetPlayers()) do
        if Player == Client then continue end

        if Player.Character and Player.Character:FindFirstChild('HumanoidRootPart') then
            local HRP = Player.Character.HumanoidRootPart
            local Humanoid = Player.Character:FindFirstChild('Humanoid')

            if Humanoid and Humanoid.Health > 0 then
                if hitboxEnabled then
                    HRP.Size = Vector3.new(sizeX, sizeY, sizeZ)
                else
                    HRP.Size = Vector3.new(2, 2, 1)
                end
                
                HRP.Transparency = hitBoxTransparency
                HRP.CanCollide = false
                HRP.Color = Color3.fromRGB(255, 0, 0)
                local lineThickness = (1 - hitBoxTransparency) * 0.08
                local areLinesVisible = hitBoxTransparency < 1

                if not HRP:FindFirstChild('SelectionBox') then
                    local outline = Instance.new('SelectionBox')
                    outline.Name = 'SelectionBox'
                    outline.Parent = HRP
                    outline.Adornee = HRP
                    outline.LineThickness = lineThickness
                    outline.Visible = areLinesVisible
                    outline.Color3 = Color3.fromRGB(0, 0, 0)
                else
                    local outline = HRP:FindFirstChild('SelectionBox')
                    outline.LineThickness = lineThickness
                    outline.Visible = areLinesVisible
                end
            else
                HRP.Size = Vector3.new(2, 2, 1)
                HRP.Transparency = 0

                local outline = HRP:FindFirstChild('SelectionBox')
                if outline then
                    outline:Destroy()
                end
            end
        end
    end
end)

game:GetService('UserInputService').InputBegan:Connect(function(input, processed)
    if processed then return end
    if input.KeyCode == Enum.KeyCode.N then
        ScreenGui.Enabled = not ScreenGui.Enabled
    end
end)

